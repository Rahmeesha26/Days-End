const button = document.querySelector('button', 'restart');

button.addEventListener('start', (event) => {
    onclick = (event) => {};
});
button.addEventListener('restart',  (event) => {
    onclick = (event) => {};
});